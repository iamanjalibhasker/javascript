// . Create a new array whose elements are in uppercase of words present in the original array.

let string = ["anjali","aman","misty","ranu","aditya"];

 const element = string.map((string) => (string.toUpperCase()));
 console.log(element);
