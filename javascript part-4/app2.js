// Create an object representing a car that  stores the following properties for the car:name, model,color.Printthecar’s name.

const car = {
     name : "ferrari",
     model : "ferrari protofino",
     color : "yellow"
};

console.log(car.name);