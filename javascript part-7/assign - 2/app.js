// Create a new array using the map function whose each element is equal to the original element plus 5.

 let nums = [2,3,4,5,6];

  const plus = nums.map((nums) => (nums + 5));

   console.log(plus);
 
